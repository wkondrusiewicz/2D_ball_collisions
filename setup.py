#!/usr/bin/env python
from setuptools import setup, find_packages
setup(
    name='ball_collisions',
    version='0.1',
    description='2D collisions of balls using classical mechanics',
    packages=find_packages(include=['ball_collisions']),
    install_requires=[
    ],
)
